import React from 'react'
import './Toolbar.css'
import Logo from '../../Logo/Logo';

const toolbar = (props) => (
    <header className="toolbar">
        <div>MENU</div>
        <Logo />
        <nav>
            <ul>

            </ul>
        </nav>
    </header>
)

export default toolbar